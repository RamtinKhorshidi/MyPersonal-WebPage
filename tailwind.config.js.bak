/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {
            colors: {
                background: "#0F0F11", // Very dark (almost black)
                surface: "#18181B",    // Slightly lighter for cards
                primary: "#D4AF37",    // Gold (Showman/Barista)
                secondary: "#61DAFB",  // Cyan (React/Tech)
                accent: "#10B981",     // Emerald (Start/Go/Nature)
                coffee: "#6F4E37",     // Deep Coffee (Barista subtle)
            },
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
                heading: ['Outfit', 'sans-serif'], // Modern, bold heading font
            },
            animation: {
                'fade-in': 'fadeIn 0.5s ease-out forwards',
                'slide-up': 'slideUp 0.5s ease-out forwards',
            },
            keyframes: {
                fadeIn: {
                    '0%': { opacity: '0' },
                    '100%': { opacity: '1' },
                },
                slideUp: {
                    '0%': { transform: 'translateY(20px)', opacity: '0' },
                    '100%': { transform: 'translateY(0)', opacity: '1' },
                }
            }
        },
    },
    plugins: [],
}
